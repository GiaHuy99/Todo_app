{
  "name": "todo-app",
  "private": true,
  "scripts": {
    "build:frontend": "npm ci --prefix frontend && npm run build --prefix frontend && npm run verify:dist --prefix frontend",
    "deploy": "npm run build:frontend && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
